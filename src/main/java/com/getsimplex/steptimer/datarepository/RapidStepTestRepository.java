package com.getsimplex.steptimer.datarepository;

import com.getsimplex.steptimer.model.RapidStepTest;

public class RapidStepTestRepository extends GenericRepository<RapidStepTest>{//using an abstract class so we can infer the type more easily using getGenericSuperClass() method
}
