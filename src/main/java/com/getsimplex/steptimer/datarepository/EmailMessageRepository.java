package com.getsimplex.steptimer.datarepository;

import com.getsimplex.steptimer.model.EmailMessage;

public class EmailMessageRepository extends GenericRepository<EmailMessage>{//using an abstract class so we can infer the type more easily using getGenericSuperClass() method
}
