package com.getsimplex.steptimer.datarepository;

import com.getsimplex.steptimer.model.Customer;

public class CustomerRepository extends GenericRepository<Customer>{//using an abstract class so we can infer the type more easily using getGenericSuperClass() method
}
